#include "include/Detection.h"
#include "include/Square.h"
#include "include/Line.h"
#include <SDL2/SDL.h>
#include <iostream>
#include <iterator>
using namespace std;


void Detection::contact(Square a, Line b) {
    //if(a.getY() + 50 > b.getY()){
        //a.update(a.getX(), 500);
       // cout << "LALA" << endl;
       //cout << a.x << " " << a.y << endl;
    //}
        
}
